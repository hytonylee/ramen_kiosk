import React, { Component } from 'react',
import { Menu, Item, Topping } from '../lib/requests';
import MenuNew from './MenuNew';


class MenuShow extends Component {
  constructor(props) {
    super(props);
    const menuId = this.props.match.params.menuId;

    this.state = {

    }
  }
}
