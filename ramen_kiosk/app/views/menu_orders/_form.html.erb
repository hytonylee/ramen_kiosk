
  <% if menu_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(menu_order.errors.count, "error") %> prohibited this menu_order from being saved:</h2>

      <ul>
      <% menu_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id, id: :menu_order_item_id %>
  </div>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id, id: :menu_order_order_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
